// gets numbers, creates sum

#include <iostream>
using namespace std;

int main() {
  int sum;

  int array[5];

  for (int x : array) {
    cin >> x;
  }
  output = sum(array);
  cout << "The sum is" << sum;
  return 0;
}
